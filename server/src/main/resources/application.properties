spring.application.name=server

# PostgreSQL connection settings
spring.datasource.url=jdbc:postgresql://postgresql-hawktu.alwaysdata.net/hawktu_logistics
spring.datasource.username=hawktu_admin
spring.datasource.password=hawktuadmin3

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Allow all origins (for development only, not recommended for production)
# spring.web.cors.allowed-origin-patterns=*
# spring.web.cors.allow-credentials=true

# Optionally restrict to specific origins, e.g., localhost:3000
# spring.web.cors.allowed-origin-patterns=http://localhost:3000
